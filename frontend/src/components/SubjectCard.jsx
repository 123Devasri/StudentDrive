import { Link } from 'react-router-dom';
function SubjectCard({ subject }) { return <article className="subject-card"><div className="d-flex justify-content-between align-items-start"><span className="subject-symbol" style={{ backgroundColor: subject.color }}>{subject.shortName}</span><span className={`status-dot ${subject.status === 'Needs attention' ? 'warning' : ''}`}>{subject.status}</span></div><h3>{subject.name}</h3><p className="muted">{subject.topics} syllabus topics</p><div className="progress-meta"><span>Coverage</span><strong>{subject.coverage}%</strong></div><div className="progress thin"><div className="progress-bar" style={{ width: `${subject.coverage}%`, backgroundColor: subject.color }} /></div><div className="card-footer-row"><span><i className="bi bi-calendar3" /> {subject.examDate}</span><Link to={`/subjects/${subject.id}`}>Open subject <i className="bi bi-arrow-up-right" /></Link></div></article>; }
export default SubjectCard;
