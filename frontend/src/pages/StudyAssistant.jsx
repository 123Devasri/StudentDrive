import { useState } from 'react';
import ChatMessage from '../components/ChatMessage';
import { chatMessages } from '../data/mockData';
function StudyAssistant() { const [messages, setMessages] = useState(chatMessages); const [input, setInput] = useState(''); const sendMessage = (event) => { event.preventDefault(); if (!input.trim()) return; setMessages([...messages, { role: 'student', text: input }]); setInput(''); }; return <div className="page-container assistant-page"><div className="page-heading"><div><p className="eyebrow">YOUR LEARNING COMPANION</p><h1>AI Study Assistant</h1><p className="lead-copy">Ask questions based on your uploaded academic resources.</p></div><span className="ai-status"><i className="bi bi-circle-fill" /> Ready to help</span></div><div className="assistant-layout"><section className="chat-panel panel"><div className="chat-header"><div className="ai-avatar"><i className="bi bi-stars" /></div><div><strong>StudentDrive AI</strong><span>Grounded in your study material</span></div></div><div className="chat-body">{messages.map((message, index) => <ChatMessage key={index} message={message} />)}{messages.length > 1 && <div className="sources"><span>Sources used</span><button><i className="bi bi-file-earmark-pdf" /> AVL_Trees.pdf</button><button><i className="bi bi-file-earmark-slides" /> Trees_Unit2.pptx</button></div>}</div><form className="chat-input" onSubmit={sendMessage}><input value={input} onChange={(event) => setInput(event.target.value)} placeholder="Ask something about your study material..." /><button aria-label="Send message"><i className="bi bi-arrow-up" /></button></form></section><aside className="suggestions panel"><span className="eyebrow">TRY ASKING</span><h2>Start with a prompt</h2>{['Explain this topic simply', 'Give me an example', 'Compare two concepts', 'Create a quiz from this topic'].map((suggestion) => <button key={suggestion} onClick={() => setInput(suggestion)}>{suggestion}<i className="bi bi-arrow-up-right" /></button>)}</aside></div></div>; }
export default StudyAssistant;
