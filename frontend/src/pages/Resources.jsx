import { useState } from 'react';
import ResourceCard from '../components/ResourceCard';
import { resources } from '../data/mockData';
function Resources() { const [filter, setFilter] = useState('All'); const visible = filter === 'All' ? resources : resources.filter((resource) => resource.type === filter); return <div className="page-container"><div className="page-heading"><div><p className="eyebrow">ACADEMIC LIBRARY</p><h1>Resources</h1><p className="lead-copy">All your study material, connected to what you need to learn.</p></div><div className="button-group"><button className="secondary-button"><i className="bi bi-folder-plus" /> New folder</button><button className="primary-button"><i className="bi bi-upload" /> Upload resource</button></div></div><div className="resource-toolbar"><div className="search-box"><i className="bi bi-search" /><input placeholder="Search resources..." /></div><div className="filter-tabs">{['All', 'PDF', 'PPT', 'DOC'].map((item) => <button className={filter === item ? 'selected' : ''} key={item} onClick={() => setFilter(item)}>{item}</button>)}</div></div><div className="folder-strip"><div className="folder-tile"><i className="bi bi-folder2-open" /><strong>Data Structures</strong><span>12 resources</span></div>{['Unit 1', 'Unit 2', 'Unit 3', 'Previous Year Papers'].map((folder) => <div className="folder-tile small" key={folder}><i className="bi bi-folder" /><strong>{folder}</strong><span>Folder</span></div>)}</div><section className="panel resource-panel"><div className="resource-list-header"><span>Name</span><span>Uploaded</span><span>Actions</span></div>{visible.map((resource) => <ResourceCard key={resource.id} resource={resource} />)}</section></div>; }
export default Resources;
